/* src/global_styles.css */
:root {
  --primary: #0f8f7f;
  --primary-dark: #0a6d61;
  --primary-light: #2dd4bf;
  --secondary: #10231f;
  --accent: #f59e0b;
  --mushroom: #c2410c;
  --mushroom-soft: #fff1e8;
  --valley: #193b2c;
  --success: #22c55e;
  --warning: #eab308;
  --danger: #ef4444;
  --gray-50: #f6f8f7;
  --gray-100: #edf2ef;
  --gray-200: #dce6e1;
  --gray-300: #c4d3cd;
  --gray-400: #8ea39a;
  --gray-500: #61746c;
  --gray-600: #46584f;
  --gray-700: #2d3d35;
  --gray-800: #1b2a24;
  --gray-900: #0b1713;
  --white: #ffffff;
  --surface: rgba(255, 255, 255, 0.88);
  --surface-strong: #ffffff;
  --surface-muted: #f8faf8;
  --border-soft: rgba(11, 23, 19, 0.09);
  --shadow-sm: 0 1px 2px rgb(11 23 19 / 0.06);
  --shadow: 0 14px 34px -26px rgb(11 23 19 / 0.34);
  --shadow-md: 0 20px 46px -30px rgb(11 23 19 / 0.42);
  --shadow-lg: 0 28px 70px -34px rgb(11 23 19 / 0.48);
  --radius-sm: 6px;
  --radius: 8px;
  --radius-lg: 10px;
  --radius-xl: 12px;
}
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
html {
  scroll-behavior: smooth;
}
body {
  font-family:
    "Inter",
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    Roboto,
    Oxygen,
    Ubuntu,
    sans-serif;
  background:
    radial-gradient(
      circle at top left,
      rgba(45, 212, 191, 0.16),
      transparent 30rem),
    linear-gradient(
      180deg,
      #fbfdfc 0%,
      var(--gray-50) 42%,
      #eef4f0 100%);
  color: var(--gray-800);
  line-height: 1.6;
  min-height: 100vh;
  text-rendering: optimizeLegibility;
}
app-root {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}
.main-content {
  flex: 1;
  padding: 0;
}
.header {
  background: var(--white);
  border-bottom: 1px solid var(--gray-200);
  position: sticky;
  top: 0;
  z-index: 100;
  box-shadow: var(--shadow-sm);
}
.header-content {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0.75rem 1.5rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
}
.logo {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  text-decoration: none;
  color: var(--gray-900);
  font-weight: 700;
  font-size: 1.25rem;
}
.logo-icon {
  font-size: 1.5rem;
}
.logo-text {
  background:
    linear-gradient(
      135deg,
      var(--primary),
      var(--primary-dark));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
.nav {
  display: flex;
  gap: 0.5rem;
}
.nav a {
  padding: 0.5rem 1rem;
  text-decoration: none;
  color: var(--gray-600);
  border-radius: var(--radius);
  font-weight: 500;
  transition: all 0.2s;
}
.nav a:hover {
  background: var(--gray-100);
  color: var(--gray-900);
}
.nav a.active {
  background: var(--primary);
  color: var(--white);
}
.user-section {
  display: flex;
  align-items: center;
  gap: 1rem;
}
.user-info {
  display: flex;
  align-items: center;
  gap: 0.75rem;
}
.user-avatar {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  object-fit: cover;
  border: 2px solid var(--gray-200);
}
.user-name {
  font-weight: 500;
  color: var(--gray-700);
}
.footer {
  background: var(--gray-900);
  color: var(--gray-300);
  margin-top: auto;
}
.footer-content {
  max-width: 1280px;
  margin: 0 auto;
  padding: 3rem 1.5rem;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 2rem;
}
.footer-section h4 {
  color: var(--white);
  margin-bottom: 1rem;
  font-size: 1.1rem;
}
.footer-section p {
  margin-bottom: 0.5rem;
  font-size: 0.9rem;
}
.footer-bottom {
  border-top: 1px solid var(--gray-700);
  padding: 1.5rem;
  text-align: center;
  font-size: 0.9rem;
}
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 0.625rem 1.25rem;
  font-size: 0.9rem;
  font-weight: 500;
  border-radius: var(--radius);
  border: none;
  cursor: pointer;
  transition: all 0.2s;
  text-decoration: none;
  white-space: nowrap;
}
.btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}
.btn-primary {
  background: var(--primary);
  color: var(--white);
}
.btn-primary:hover:not(:disabled) {
  background: var(--primary-dark);
  transform: translateY(-1px);
  box-shadow: var(--shadow-md);
}
.btn-outline {
  background: transparent;
  color: var(--gray-700);
  border: 1px solid var(--gray-300);
}
.btn-outline:hover:not(:disabled) {
  background: var(--gray-100);
  border-color: var(--gray-400);
}
.btn-success {
  background: var(--success);
  color: var(--white);
}
.btn-success:hover:not(:disabled) {
  background: #16a34a;
}
.btn-warning {
  background: var(--warning);
  color: var(--gray-900);
}
.btn-warning:hover:not(:disabled) {
  background: #ca8a04;
}
.btn-danger {
  background: var(--danger);
  color: var(--white);
}
.btn-danger:hover:not(:disabled) {
  background: #dc2626;
}
.btn-sm {
  padding: 0.375rem 0.75rem;
  font-size: 0.8rem;
}
.btn-lg {
  padding: 0.875rem 1.75rem;
  font-size: 1rem;
}
.btn-block {
  width: 100%;
}
.btn-disabled {
  background: var(--gray-300);
  color: var(--gray-500);
  cursor: not-allowed;
}
.form-group {
  margin-bottom: 1.25rem;
}
.form-group label {
  display: block;
  margin-bottom: 0.5rem;
  font-weight: 500;
  color: var(--gray-700);
  font-size: 0.9rem;
}
.input,
.select,
.textarea {
  width: 100%;
  padding: 0.75rem 1rem;
  font-size: 0.95rem;
  border: 1px solid var(--gray-300);
  border-radius: var(--radius);
  background: var(--white);
  color: var(--gray-800);
  transition: all 0.2s;
}
.input:focus,
.select:focus,
.textarea:focus {
  outline: none;
  border-color: var(--primary);
  box-shadow: 0 0 0 3px rgba(13, 148, 136, 0.1);
}
.textarea {
  resize: vertical;
  min-height: 80px;
}
.select {
  cursor: pointer;
}
.select-sm {
  padding: 0.375rem 0.5rem;
  font-size: 0.8rem;
  border: 1px solid var(--gray-300);
  border-radius: var(--radius-sm);
  background: var(--white);
}
.select-sm.payment-paid {
  border-color: #16a34a;
  background: #dcfce7;
  color: #166534;
  font-weight: 600;
}
.select-sm.payment-pending {
  border-color: #ca8a04;
  background: #fef3c7;
  color: #92400e;
  font-weight: 600;
}
.hero {
  background:
    linear-gradient(
      135deg,
      var(--secondary) 0%,
      var(--gray-800) 100%);
  color: var(--white);
  padding: 6rem 1.5rem;
  text-align: center;
}
.hero h1 {
  font-size: 3rem;
  font-weight: 800;
  margin-bottom: 1rem;
  line-height: 1.2;
}
.hero p {
  font-size: 1.25rem;
  opacity: 0.9;
  margin-bottom: 2rem;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}
.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 2rem;
}
.section-header h2 {
  font-size: 1.75rem;
  font-weight: 700;
  color: var(--gray-900);
}
.features {
  padding: 4rem 1.5rem;
  max-width: 1280px;
  margin: 0 auto;
}
.features h2 {
  text-align: center;
}
.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2rem;
  margin-top: 2rem;
}
.feature-card {
  background: var(--white);
  padding: 2rem;
  border-radius: var(--radius-xl);
  text-align: center;
  box-shadow: var(--shadow);
  transition: transform 0.2s;
}
.feature-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
}
.feature-icon {
  font-size: 3rem;
  margin-bottom: 1rem;
}
.feature-card h3 {
  font-size: 1.1rem;
  margin-bottom: 0.75rem;
  color: var(--gray-800);
}
.feature-card p {
  color: var(--gray-500);
  font-size: 0.95rem;
}
.popular-courts {
  padding: 4rem 1.5rem;
  max-width: 1280px;
  margin: 0 auto;
  background: var(--gray-100);
}
.courts-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 1.5rem;
}
.court-card {
  background: var(--white);
  border-radius: var(--radius-xl);
  overflow: hidden;
  box-shadow: var(--shadow);
  transition: all 0.3s;
}
.court-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
}
.court-card.inactive {
  opacity: 0.7;
}
.court-image {
  position: relative;
  height: 200px;
  overflow: hidden;
}
.court-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.3s;
}
.court-card:hover .court-image img {
  transform: scale(1.05);
}
.court-type {
  position: absolute;
  top: 1rem;
  left: 1rem;
  background: var(--primary);
  color: var(--white);
  padding: 0.375rem 0.75rem;
  border-radius: var(--radius-sm);
  font-size: 0.8rem;
  font-weight: 600;
  text-transform: capitalize;
}
.inactive-badge {
  position: absolute;
  top: 1rem;
  right: 1rem;
  background: var(--gray-600);
  color: var(--white);
  padding: 0.375rem 0.75rem;
  border-radius: var(--radius-sm);
  font-size: 0.8rem;
  font-weight: 600;
}
.court-info {
  padding: 1.25rem;
}
.court-info h3 {
  font-size: 1.15rem;
  margin-bottom: 0.5rem;
  color: var(--gray-800);
}
.court-description {
  color: var(--gray-500);
  font-size: 0.9rem;
  margin-bottom: 1rem;
  line-height: 1.5;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
.court-amenities {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-bottom: 1rem;
}
.amenity-tag {
  background: var(--gray-100);
  color: var(--gray-600);
  padding: 0.25rem 0.5rem;
  border-radius: var(--radius-sm);
  font-size: 0.75rem;
}
.amenity-badge {
  background: var(--primary-light);
  color: var(--white);
  padding: 0.5rem 1rem;
  border-radius: var(--radius);
  font-size: 0.85rem;
  font-weight: 500;
}
.amenity-more {
  color: var(--gray-400);
  font-size: 0.75rem;
  padding: 0.25rem 0.5rem;
}
.court-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 1rem;
  border-top: 1px solid var(--gray-100);
}
.court-price {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--primary);
}
.court-meta {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}
.court-players {
  font-size: 0.85rem;
  color: var(--gray-500);
}
.stats {
  background:
    linear-gradient(
      135deg,
      var(--primary) 0%,
      var(--primary-dark) 100%);
  color: var(--white);
  padding: 4rem 1.5rem;
}
.stats-grid {
  max-width: 1280px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 2rem;
  text-align: center;
}
.stat-item {
  padding: 1.5rem;
}
.stat-number {
  font-size: 3rem;
  font-weight: 800;
  display: block;
}
.stat-label {
  font-size: 1rem;
  opacity: 0.9;
}
.page-header {
  background:
    linear-gradient(
      135deg,
      var(--secondary) 0%,
      var(--gray-800) 100%);
  color: var(--white);
  padding: 3rem 1.5rem;
  text-align: center;
}
.page-header h1 {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}
.page-header p {
  opacity: 0.9;
  font-size: 1.1rem;
}
.filters {
  max-width: 1280px;
  margin: 0 auto;
  padding: 1.5rem;
  display: flex;
  flex-wrap: wrap;
  gap: 1.5rem;
  align-items: center;
}
.filter-group {
  display: flex;
  align-items: center;
  gap: 0.75rem;
}
.filter-group label {
  font-weight: 500;
  color: var(--gray-600);
}
.filter-buttons {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;
}
.courts-section {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 1.5rem 3rem;
}
.court-detail {
  max-width: 1000px;
  margin: 0 auto;
  padding: 2rem 1.5rem;
}
.court-header {
  margin-bottom: 2rem;
}
.back-link {
  display: inline-block;
  color: var(--primary);
  text-decoration: none;
  margin-bottom: 1rem;
  font-weight: 500;
}
.back-link:hover {
  text-decoration: underline;
}
.court-image-large {
  position: relative;
  width: 100%;
  height: 400px;
  border-radius: var(--radius-xl);
  overflow: hidden;
  margin-bottom: 1.5rem;
}
.court-image-large img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.court-type-badge {
  position: absolute;
  top: 1.5rem;
  left: 1.5rem;
  background: var(--primary);
  color: var(--white);
  padding: 0.5rem 1.25rem;
  border-radius: var(--radius);
  font-size: 0.9rem;
  font-weight: 600;
  text-transform: capitalize;
}
.court-title-section h1 {
  font-size: 2rem;
  margin-bottom: 0.75rem;
}
.court-description-full {
  color: var(--gray-600);
  font-size: 1.1rem;
  margin-bottom: 1.5rem;
}
.court-quick-info {
  display: flex;
  gap: 2rem;
  flex-wrap: wrap;
}
.info-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  color: var(--gray-600);
}
.info-item.inactive-status {
  color: var(--danger);
}
.court-amenities-section {
  background: var(--white);
  padding: 1.5rem;
  border-radius: var(--radius-lg);
  margin-bottom: 2rem;
}
.court-amenities-section h2 {
  margin-bottom: 1rem;
  font-size: 1.25rem;
}
.amenities-list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
}
.booking-section {
  background: var(--white);
  padding: 2rem;
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-md);
}
.booking-section h2 {
  margin-bottom: 1.5rem;
  font-size: 1.5rem;
}
.booking-form {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}
.time-slots {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 0.75rem;
}
.time-slot {
  padding: 0.75rem 1rem;
  border: 1px solid var(--gray-300);
  border-radius: var(--radius);
  background: var(--white);
  cursor: pointer;
  font-size: 0.9rem;
  transition: all 0.2s;
}
.time-slot.available:hover {
  border-color: var(--primary);
  background: rgba(13, 148, 136, 0.05);
}
.time-slot.selected {
  background: var(--primary);
  border-color: var(--primary);
  color: var(--white);
}
.time-slot:disabled {
  background: var(--gray-100);
  color: var(--gray-400);
  cursor: not-allowed;
  text-decoration: line-through;
}
.selection-info {
  margin-top: 0.75rem;
  padding: 0.75rem;
  background: var(--primary-light);
  color: var(--white);
  border-radius: var(--radius);
  font-weight: 500;
}
.booking-summary {
  background: var(--gray-50);
  padding: 1.25rem;
  border-radius: var(--radius);
}
.payment-options {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 0.75rem;
}
.payment-option {
  display: flex;
  gap: 0.75rem;
  align-items: flex-start;
  padding: 1rem;
  border: 1px solid var(--gray-300);
  border-radius: var(--radius);
  background: var(--white);
  cursor: pointer;
}
.payment-option.selected {
  border-color: var(--primary);
  background: rgba(13, 148, 136, 0.05);
}
.payment-option input {
  margin-top: 0.25rem;
}
.payment-option small {
  display: block;
  color: var(--gray-500);
  margin-top: 0.2rem;
}
.summary-row {
  display: flex;
  justify-content: space-between;
  margin-bottom: 0.5rem;
  color: var(--gray-600);
}
.summary-row.total {
  margin-top: 0.75rem;
  padding-top: 0.75rem;
  border-top: 1px solid var(--gray-200);
  font-weight: 700;
  font-size: 1.15rem;
  color: var(--gray-800);
}
.login-prompt {
  text-align: center;
  padding: 1.5rem;
  background: var(--gray-50);
  border-radius: var(--radius);
}
.login-prompt p {
  margin-bottom: 1rem;
  color: var(--gray-600);
}
.booking-contact,
.guest-booking {
  background: var(--gray-50);
  padding: 1.25rem;
  border-radius: var(--radius);
}
.contact-label {
  color: var(--gray-500);
  font-size: 0.85rem;
  margin-bottom: 0.25rem;
}
.contact-value {
  color: var(--gray-800);
  font-weight: 600;
}
.guest-note {
  margin-top: 0.75rem;
  color: var(--gray-500);
  font-size: 0.9rem;
  text-align: center;
}
.guest-note a {
  color: var(--primary);
  font-weight: 600;
}
.form-error {
  margin-top: 0.5rem;
  color: var(--danger);
  font-size: 0.85rem;
}
.guest-badge {
  display: inline-block;
  margin-left: 0.5rem;
  padding: 0.125rem 0.4rem;
  border-radius: var(--radius-sm);
  background: var(--gray-100);
  color: var(--gray-600);
  font-size: 0.7rem;
  font-weight: 600;
}
.no-slots {
  color: var(--gray-500);
  text-align: center;
  padding: 1rem;
}
.success-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 200;
  padding: 1rem;
}
.success-modal {
  background: var(--white);
  padding: 2rem;
  border-radius: var(--radius-xl);
  max-width: 400px;
  width: 100%;
  text-align: center;
}
.success-icon {
  width: 60px;
  height: 60px;
  background: var(--success);
  color: var(--white);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 1rem;
  font-size: 2rem;
}
.success-modal h2 {
  margin-bottom: 0.5rem;
}
.email-confirmation-note {
  margin: 1rem 0;
  color: var(--gray-500);
  font-size: 0.9rem;
}
.success-details {
  background: var(--gray-50);
  padding: 1rem;
  border-radius: var(--radius);
  margin: 1.5rem 0;
  text-align: left;
}
.success-details p {
  margin-bottom: 0.5rem;
}
.success-modal button {
  margin: 0.25rem;
}
.auth-page {
  min-height: calc(100vh - 200px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 2rem;
}
.auth-card {
  background: var(--white);
  padding: 2.5rem;
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-lg);
  width: 100%;
  max-width: 420px;
}
.auth-header {
  text-align: center;
  margin-bottom: 2rem;
}
.auth-header h1 {
  font-size: 1.75rem;
  margin-bottom: 0.5rem;
}
.auth-header p {
  color: var(--gray-500);
}
.auth-form {
  margin-bottom: 1.5rem;
}
.error-message {
  background: #fef2f2;
  color: var(--danger);
  padding: 0.75rem;
  border-radius: var(--radius);
  margin-bottom: 1rem;
  font-size: 0.9rem;
  border: 1px solid #fee2e2;
}
.auth-footer {
  border-top: 1px solid var(--gray-200);
  padding-top: 1.5rem;
}
.demo-credentials {
  background: var(--gray-50);
  padding: 1rem;
  border-radius: var(--radius);
}
.demo-title {
  font-weight: 600;
  margin-bottom: 0.75rem;
  color: var(--gray-700);
}
.demo-user p {
  font-size: 0.85rem;
  color: var(--gray-600);
  margin-bottom: 0.25rem;
}
.reservations-section {
  max-width: 900px;
  margin: 0 auto;
  padding: 2rem 1.5rem;
}
.reservations-tabs {
  display: flex;
  gap: 0.5rem;
  margin-bottom: 1.5rem;
}
.tab-btn {
  padding: 0.625rem 1.25rem;
  background: var(--white);
  border: 1px solid var(--gray-200);
  border-radius: var(--radius);
  cursor: pointer;
  font-weight: 500;
  color: var(--gray-600);
  transition: all 0.2s;
}
.tab-btn:hover {
  background: var(--gray-50);
}
.tab-btn.active {
  background: var(--primary);
  border-color: var(--primary);
  color: var(--white);
}
.reservations-list {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}
.reservation-card {
  background: var(--white);
  padding: 1.5rem;
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow);
}
.reservation-card.cancelled {
  opacity: 0.7;
  background: var(--gray-50);
}
.reservation-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 1rem;
}
.reservation-title h3 {
  font-size: 1.1rem;
  margin-bottom: 0.5rem;
}
.reservation-id {
  color: var(--gray-400);
  font-size: 0.85rem;
}
.reservation-status {
  padding: 0.25rem 0.75rem;
  border-radius: var(--radius-sm);
  font-size: 0.8rem;
  font-weight: 600;
}
.reservation-status.confirmed {
  background: #dcfce7;
  color: #16a34a;
}
.reservation-status.cancelled {
  background: #fee2e2;
  color: #dc2626;
}
.reservation-status.completed {
  background: var(--gray-100);
  color: var(--gray-600);
}
.reservation-details {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 0.75rem;
  margin-bottom: 1rem;
}
.detail-item {
  display: flex;
  justify-content: space-between;
}
.detail-label {
  color: var(--gray-500);
}
.detail-value {
  font-weight: 500;
}
.detail-value.price {
  color: var(--primary);
  font-weight: 700;
}
.reservation-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 1rem;
  border-top: 1px solid var(--gray-100);
}
.reservation-date {
  color: var(--gray-400);
  font-size: 0.85rem;
}
.no-reservations {
  text-align: center;
  padding: 3rem;
  background: var(--white);
  border-radius: var(--radius-lg);
}
.no-reservations h3 {
  margin-bottom: 0.5rem;
}
.no-reservations p {
  color: var(--gray-500);
  margin-bottom: 1rem;
}
.login-prompt-center {
  text-align: center;
  padding: 4rem 2rem;
}
.login-prompt-center h3 {
  margin-bottom: 1rem;
}
.login-prompt-center a {
  margin-top: 1rem;
}
.admin-panel {
  max-width: 1400px;
  margin: 0 auto;
  padding: 2rem 1.5rem;
}
.admin-schedule {
  display: grid;
  gap: 1rem;
}
.schedule-hero {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 1rem;
  padding: 1.5rem;
  background:
    linear-gradient(
      135deg,
      #0f172a 0%,
      #17364b 62%,
      #0f766e 100%);
  color: var(--white);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-md);
}
.schedule-eyebrow {
  margin-bottom: 0.35rem;
  color: #99f6e4;
  font-size: 0.78rem;
  font-weight: 800;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}
.schedule-hero h2 {
  margin-bottom: 0.35rem;
  font-size: clamp(1.6rem, 3vw, 2.4rem);
  line-height: 1.05;
}
.schedule-hero p {
  max-width: 660px;
  color: rgba(255, 255, 255, 0.82);
}
.schedule-actions {
  display: flex;
  gap: 0.75rem;
  flex-wrap: wrap;
  justify-content: flex-end;
}
.schedule-actions .btn-outline {
  color: var(--white);
  border-color: rgba(255, 255, 255, 0.34);
}
.schedule-stats {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 1rem;
}
.schedule-stat {
  padding: 1.15rem;
  background: var(--white);
  border: 1px solid var(--gray-200);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
}
.schedule-stat span,
.schedule-stat small {
  display: block;
  color: var(--gray-500);
  font-size: 0.82rem;
}
.schedule-stat strong {
  display: block;
  margin: 0.35rem 0;
  color: var(--gray-900);
  font-size: 2rem;
  line-height: 1;
}
.schedule-layout {
  display: grid;
  grid-template-columns: minmax(0, 1.45fr) minmax(300px, 0.55fr);
  gap: 1rem;
  align-items: start;
}
.schedule-board,
.side-card {
  background: var(--white);
  border: 1px solid var(--gray-200);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow);
}
.schedule-toolbar {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  padding: 1rem;
  border-bottom: 1px solid var(--gray-200);
}
.schedule-date,
.schedule-filter {
  display: grid;
  gap: 0.35rem;
  min-width: 180px;
}
.schedule-date label,
.schedule-filter label {
  color: var(--gray-500);
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}
.schedule-calendar {
  padding: 1rem;
  overflow-x: auto;
}
.schedule-mobile {
  display: none;
}
.schedule-grid {
  min-width: 820px;
  display: grid;
  border: 1px solid var(--gray-200);
  border-radius: var(--radius-lg);
  overflow: hidden;
}
.schedule-cell {
  min-height: 64px;
  padding: 0.55rem;
  border-right: 1px solid var(--gray-200);
  border-bottom: 1px solid var(--gray-200);
  background: var(--white);
}
.schedule-cell:nth-child(6n) {
  border-right: 0;
}
.schedule-head {
  min-height: 54px;
  background: var(--gray-50);
  color: var(--gray-700);
  font-size: 0.78rem;
  font-weight: 800;
}
.schedule-head strong,
.schedule-head span {
  display: block;
}
.schedule-head span {
  margin-top: 0.2rem;
  color: var(--gray-500);
  font-weight: 600;
  text-transform: capitalize;
}
.schedule-time {
  background: var(--gray-50);
  color: var(--gray-500);
  font-size: 0.78rem;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
}
.schedule-booking,
.schedule-empty {
  width: 100%;
  min-height: 46px;
  border: 0;
  border-radius: var(--radius);
  text-align: left;
  cursor: pointer;
}
.schedule-booking {
  padding: 0.55rem 0.65rem;
  background: var(--primary);
  color: var(--white);
  box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.18);
}
.schedule-booking.paid {
  background: #0d9488;
}
.schedule-booking.pending {
  background: #f59e0b;
  color: #422006;
}
.schedule-booking.completed {
  background: #475569;
}
.schedule-booking strong,
.schedule-booking span,
.schedule-booking small {
  display: block;
}
.schedule-booking strong {
  font-size: 0.82rem;
  line-height: 1.15;
}
.schedule-booking span,
.schedule-booking small {
  opacity: 0.88;
  font-size: 0.72rem;
}
.schedule-empty {
  padding: 0.55rem;
  background: transparent;
  color: var(--gray-400);
  font-size: 0.78rem;
}
.schedule-empty:hover {
  background: #ecfdf5;
  color: var(--primary-dark);
}
.schedule-side {
  display: grid;
  gap: 1rem;
}
.side-card {
  padding: 1rem;
}
.side-card h3 {
  margin-bottom: 1rem;
  color: var(--gray-900);
  font-size: 1rem;
}
.detail-stack {
  display: grid;
  gap: 0.65rem;
}
.detail-stack p {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  padding-bottom: 0.65rem;
  border-bottom: 1px solid var(--gray-100);
}
.detail-stack span,
.muted-copy {
  color: var(--gray-500);
  font-size: 0.85rem;
}
.detail-stack strong {
  color: var(--gray-800);
  text-align: right;
}
.quick-row {
  display: flex;
  gap: 0.65rem;
  flex-wrap: wrap;
  margin-top: 1rem;
}
.empty-slot-note {
  margin-top: 0.9rem;
  padding: 0.8rem;
  color: #166534;
  background: #dcfce7;
  border-radius: var(--radius);
  font-weight: 700;
}
.recent-list {
  display: grid;
  gap: 0.5rem;
}
.recent-row {
  display: grid;
  grid-template-columns: 44px 1fr auto;
  align-items: center;
  gap: 0.65rem;
  width: 100%;
  padding: 0.7rem;
  border: 1px solid var(--gray-200);
  border-radius: var(--radius);
  background: var(--white);
  color: var(--gray-700);
  text-align: left;
  cursor: pointer;
}
.recent-row:hover {
  border-color: var(--primary);
  background: #f0fdfa;
}
.recent-row span {
  color: var(--gray-500);
  font-size: 0.78rem;
  font-variant-numeric: tabular-nums;
}
.recent-row strong {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.recent-row small {
  color: #166534;
  font-weight: 700;
}
.recent-row small.pending {
  color: #92400e;
}
.admin-header {
  margin-bottom: 2rem;
}
.admin-header h1 {
  font-size: 2rem;
  margin-bottom: 0.5rem;
}
.admin-tabs {
  display: flex;
  gap: 0.5rem;
  margin-bottom: 2rem;
  border-bottom: 1px solid var(--gray-200);
  padding-bottom: 1rem;
}
.admin-section {
  background: var(--white);
  padding: 1.5rem;
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow);
}
.section-actions {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 1.5rem;
}
.section-actions h2 {
  font-size: 1.25rem;
}
.filters-inline {
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-end;
  gap: 0.75rem;
}
.filters-inline .select,
.filters-inline .input {
  min-width: 150px;
  width: auto;
}
.data-table {
  width: 100%;
  border-collapse: collapse;
}
.data-table th {
  text-align: left;
  padding: 1rem;
  background: var(--gray-50);
  font-weight: 600;
  color: var(--gray-600);
  border-bottom: 2px solid var(--gray-200);
}
.data-table td {
  padding: 1rem;
  border-bottom: 1px solid var(--gray-100);
  vertical-align: middle;
}
.table-image {
  width: 80px;
  height: 50px;
  object-fit: cover;
  border-radius: var(--radius-sm);
}
.type-badge {
  background: var(--primary-light);
  color: var(--white);
  padding: 0.25rem 0.75rem;
  border-radius: var(--radius-sm);
  font-size: 0.8rem;
  text-transform: capitalize;
}
.status-badge {
  padding: 0.25rem 0.75rem;
  border-radius: var(--radius-sm);
  font-size: 0.8rem;
  font-weight: 600;
}
.status-badge.active {
  background: #dcfce7;
  color: #16a34a;
}
.status-badge.inactive {
  background: var(--gray-100);
  color: var(--gray-600);
}
.status-badge.confirmed {
  background: #dcfce7;
  color: #16a34a;
}
.status-badge.cancelled {
  background: #fee2e2;
  color: #dc2626;
}
.status-badge.completed {
  background: var(--gray-100);
  color: var(--gray-600);
}
.status-badge.paid {
  background: #dcfce7;
  color: #16a34a;
}
.status-badge.pending {
  background: #fef3c7;
  color: #ca8a04;
}
.payment-method-label {
  margin-top: 0.35rem;
  color: var(--gray-500);
  font-size: 0.8rem;
}
.actions-cell {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;
}
.admin-mobile-list {
  display: none;
}
.admin-mobile-card {
  background: var(--white);
  border: 1px solid var(--gray-200);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
}
.admin-mobile-card-top {
  display: flex;
  gap: 0.85rem;
}
.admin-mobile-thumb {
  width: 82px;
  height: 82px;
  flex: 0 0 82px;
  border-radius: var(--radius);
  object-fit: cover;
}
.admin-mobile-main {
  min-width: 0;
  flex: 1;
}
.admin-mobile-title-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 0.75rem;
}
.admin-mobile-title-row h3 {
  color: var(--gray-900);
  font-size: 1rem;
  line-height: 1.2;
}
.admin-mobile-subtitle {
  margin-top: 0.2rem;
  color: var(--gray-500);
  font-size: 0.84rem;
  font-variant-numeric: tabular-nums;
}
.admin-mobile-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 0.45rem;
  margin-top: 0.65rem;
  color: var(--gray-600);
  font-size: 0.82rem;
}
.admin-mobile-actions {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 0.55rem;
  margin-top: 0.9rem;
}
.admin-mobile-detail-grid {
  display: grid;
  gap: 0.55rem;
  margin-top: 0.9rem;
}
.admin-mobile-detail-grid p {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 1rem;
  padding-bottom: 0.55rem;
  border-bottom: 1px solid var(--gray-100);
}
.admin-mobile-detail-grid p:last-child {
  border-bottom: 0;
}
.admin-mobile-detail-grid span {
  color: var(--gray-500);
  font-size: 0.82rem;
}
.admin-mobile-detail-grid strong {
  min-width: 0;
  color: var(--gray-800);
  text-align: right;
  overflow-wrap: anywhere;
}
.admin-mobile-controls {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.75rem;
  margin-top: 0.85rem;
}
.admin-mobile-controls label {
  display: grid;
  gap: 0.35rem;
  color: var(--gray-500);
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
}
.mobile-guest-badge {
  display: inline-flex;
  margin-top: 0.6rem;
}
.admin-mobile-empty {
  padding: 1rem;
  color: var(--gray-500);
  text-align: center;
}
.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
  gap: 1rem;
  margin-bottom: 2rem;
}
.stat-card {
  background:
    linear-gradient(
      135deg,
      var(--gray-50),
      var(--gray-100));
  padding: 1.5rem;
  border-radius: var(--radius-lg);
  text-align: center;
}
.stat-card h3 {
  font-size: 0.9rem;
  color: var(--gray-500);
  margin-bottom: 0.5rem;
}
.stat-value {
  font-size: 2rem;
  font-weight: 800;
  color: var(--primary);
}
.stat-value.text {
  font-size: 1rem;
  font-weight: 600;
}
.chart-bars {
  margin-top: 1.5rem;
}
.chart-bar-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 0.75rem;
}
.bar-label {
  width: 100px;
  font-weight: 500;
  text-transform: capitalize;
}
.bar-container {
  flex: 1;
  height: 24px;
  background: var(--gray-100);
  border-radius: var(--radius-sm);
  overflow: hidden;
}
.bar-fill {
  height: 100%;
  background:
    linear-gradient(
      90deg,
      var(--primary),
      var(--primary-light));
  transition: width 0.3s;
}
.bar-value {
  width: 40px;
  text-align: right;
  font-weight: 600;
  color: var(--gray-600);
}
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 200;
  padding: 1rem;
}
.modal-content {
  background: var(--white);
  padding: 2rem;
  border-radius: var(--radius-xl);
  max-width: 550px;
  width: 100%;
  max-height: 90vh;
  overflow-y: auto;
}
.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1.5rem;
}
.modal-header h2 {
  font-size: 1.5rem;
}
.modal-close {
  background: none;
  border: none;
  font-size: 1.5rem;
  cursor: pointer;
  color: var(--gray-400);
  padding: 0.5rem;
}
.modal-close:hover {
  color: var(--gray-600);
}
.modal-form {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
}
.checkbox-group label {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  cursor: pointer;
}
.checkbox-group input[type=checkbox] {
  width: 18px;
  height: 18px;
  accent-color: var(--primary);
}
.modal-actions {
  display: flex;
  gap: 1rem;
  margin-top: 1.5rem;
}
.not-found {
  text-align: center;
  padding: 4rem 2rem;
}
.access-denied {
  text-align: center;
  padding: 4rem 2rem;
}
.access-denied h2 {
  color: var(--danger);
  margin-bottom: 1rem;
}
.access-denied p {
  color: var(--gray-500);
  margin-bottom: 1.5rem;
}
.header {
  background: rgba(255, 255, 255, 0.82);
  border-bottom: 1px solid var(--border-soft);
  box-shadow: 0 12px 28px -26px rgb(11 23 19 / 0.35);
  -webkit-backdrop-filter: blur(18px);
  backdrop-filter: blur(18px);
}
.header-content {
  padding: 0.9rem 1.5rem;
}
.logo {
  gap: 0.65rem;
  letter-spacing: 0;
}
.logo-icon {
  width: 38px;
  height: 38px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-lg);
  background:
    linear-gradient(
      135deg,
      rgba(15, 143, 127, 0.13),
      rgba(245, 158, 11, 0.16));
  box-shadow: inset 0 0 0 1px rgba(15, 143, 127, 0.14);
  font-size: 1.25rem;
}
.logo-text {
  background: none;
  color: var(--gray-900);
  -webkit-text-fill-color: currentColor;
}
.nav {
  padding: 0.25rem;
  background: rgba(237, 242, 239, 0.72);
  border: 1px solid rgba(220, 230, 225, 0.8);
  border-radius: var(--radius-lg);
}
.nav a {
  border-radius: var(--radius);
  color: var(--gray-600);
  font-size: 0.92rem;
  font-weight: 650;
}
.nav a:hover {
  background: rgba(255, 255, 255, 0.72);
  color: var(--gray-900);
}
.nav a.active {
  background: var(--white);
  color: var(--primary-dark);
  box-shadow: var(--shadow-sm);
}
.user-avatar {
  border-color: var(--white);
  box-shadow: 0 0 0 1px var(--border-soft), var(--shadow-sm);
}
.btn {
  border-radius: var(--radius);
  font-weight: 700;
  letter-spacing: 0;
  box-shadow: none;
}
.btn-primary {
  background:
    linear-gradient(
      135deg,
      var(--primary),
      var(--primary-dark));
  box-shadow: 0 12px 24px -16px rgba(15, 143, 127, 0.85);
}
.btn-primary:hover:not(:disabled) {
  background:
    linear-gradient(
      135deg,
      #11a090,
      var(--primary-dark));
  transform: translateY(-2px);
  box-shadow: 0 18px 30px -18px rgba(15, 143, 127, 0.95);
}
.btn-outline {
  background: rgba(255, 255, 255, 0.72);
  color: var(--gray-700);
  border: 1px solid var(--border-soft);
}
.btn-outline:hover:not(:disabled) {
  background: var(--white);
  border-color: rgba(15, 143, 127, 0.26);
  color: var(--primary-dark);
  transform: translateY(-1px);
}
.btn-ghost {
  background: rgba(255, 255, 255, 0.12);
  color: var(--white);
  border: 1px solid rgba(255, 255, 255, 0.22);
  -webkit-backdrop-filter: blur(10px);
  backdrop-filter: blur(10px);
}
.btn-ghost:hover:not(:disabled) {
  background: rgba(255, 255, 255, 0.18);
  border-color: rgba(255, 255, 255, 0.36);
  transform: translateY(-2px);
}
.input,
.select,
.textarea,
.select-sm {
  border-color: var(--border-soft);
  background: rgba(255, 255, 255, 0.9);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.55);
}
.input:focus,
.select:focus,
.textarea:focus {
  border-color: rgba(15, 143, 127, 0.52);
  box-shadow: 0 0 0 4px rgba(15, 143, 127, 0.12);
}
.hero,
.page-header {
  position: relative;
  overflow: hidden;
  background:
    linear-gradient(
      120deg,
      rgba(11, 23, 19, 0.9),
      rgba(18, 63, 45, 0.76) 48%,
      rgba(194, 65, 12, 0.52)),
    url(https://images.pexels.com/photos/1578750/pexels-photo-1578750.jpeg?auto=compress&cs=tinysrgb&w=1800);
  background-size: cover;
  background-position: center;
}
.hero::before,
.page-header::before {
  content: "";
  position: absolute;
  inset: 0;
  background:
    linear-gradient(
      90deg,
      rgba(255, 255, 255, 0.12) 1px,
      transparent 1px),
    linear-gradient(
      0deg,
      rgba(255, 255, 255, 0.1) 1px,
      transparent 1px);
  background-size: 48px 48px;
  opacity: 0.16;
  pointer-events: none;
}
.hero-content,
.hero-rental-card,
.page-header h1,
.page-header p {
  position: relative;
  z-index: 1;
}
.hero {
  min-height: 680px;
  padding: 6rem max(1.5rem, calc((100vw - 1200px) / 2)) 5.25rem;
  display: grid;
  grid-template-columns: minmax(0, 1fr) 380px;
  align-items: center;
  gap: clamp(2rem, 6vw, 5rem);
  text-align: left;
}
.hero h1 {
  max-width: 820px;
  margin-bottom: 1.25rem;
  font-size: clamp(3rem, 6.2vw, 6.6rem);
  letter-spacing: 0;
  line-height: 0.95;
}
.hero p {
  max-width: 650px;
  margin: 0 0 2rem;
  color: rgba(255, 255, 255, 0.86);
  font-size: clamp(1.05rem, 1.5vw, 1.35rem);
  line-height: 1.65;
}
.hero .btn-primary {
  background:
    linear-gradient(
      135deg,
      var(--mushroom),
      #f97316);
  color: var(--white);
  box-shadow: 0 22px 40px -24px rgb(0 0 0 / 0.8);
}
.hero .btn-primary:hover:not(:disabled) {
  background:
    linear-gradient(
      135deg,
      #d94c12,
      #fb923c);
  color: var(--white);
}
.hero-location {
  width: fit-content;
  display: inline-flex;
  align-items: center;
  gap: 0.65rem;
  margin-bottom: 1.35rem;
  padding: 0.45rem 0.8rem 0.45rem 0.55rem;
  border: 1px solid rgba(255, 255, 255, 0.22);
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.12);
  color: rgba(255, 255, 255, 0.9);
  font-weight: 800;
  font-size: 0.88rem;
  -webkit-backdrop-filter: blur(12px);
  backdrop-filter: blur(12px);
}
.hero-mushroom,
.rental-mark {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--mushroom-soft);
  color: var(--mushroom);
}
.hero-mushroom {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  box-shadow: inset 0 0 0 1px rgba(194, 65, 12, 0.16);
}
.hero-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.85rem;
  margin-bottom: 2rem;
}
.hero-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 0.65rem;
}
.hero-meta span {
  padding: 0.45rem 0.75rem;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.11);
  color: rgba(255, 255, 255, 0.82);
  border: 1px solid rgba(255, 255, 255, 0.15);
  font-size: 0.84rem;
  font-weight: 750;
}
.hero-rental-card {
  align-self: end;
  padding: 1.15rem;
  border: 1px solid rgba(255, 255, 255, 0.24);
  border-radius: 18px;
  background:
    linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.22),
      rgba(255, 255, 255, 0.11)),
    rgba(9, 18, 15, 0.22);
  box-shadow: 0 30px 80px -38px rgb(0 0 0 / 0.9);
  color: var(--white);
  -webkit-backdrop-filter: blur(18px);
  backdrop-filter: blur(18px);
}
.rental-card-top {
  display: flex;
  align-items: center;
  gap: 0.8rem;
  padding-bottom: 1rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.16);
}
.rental-mark {
  width: 48px;
  height: 48px;
  flex: 0 0 48px;
  border-radius: 14px;
  font-size: 1.35rem;
}
.rental-card-top strong,
.rental-card-top span,
.rental-card-main span,
.rental-card-main small {
  display: block;
}
.rental-card-top strong {
  font-size: 1rem;
}
.rental-card-top span,
.rental-card-main span,
.rental-card-main small {
  color: rgba(255, 255, 255, 0.72);
  font-size: 0.84rem;
}
.rental-card-main {
  padding: 1.25rem 0;
}
.rental-card-main strong {
  display: block;
  margin: 0.15rem 0;
  color: #fff7ed;
  font-size: 3.4rem;
  line-height: 1;
  letter-spacing: 0;
}
.rental-card-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 0.65rem;
}
.rental-card-grid span {
  padding: 0.75rem;
  border-radius: var(--radius-lg);
  background: rgba(255, 255, 255, 0.12);
  color: rgba(255, 255, 255, 0.86);
  font-weight: 800;
  text-align: center;
}
.section-header {
  gap: 1rem;
}
.section-header h2,
.admin-header h1,
.booking-section h2,
.court-title-section h1,
.auth-header h1,
.modal-header h2 {
  color: var(--gray-900);
  letter-spacing: 0;
}
.features,
.popular-courts,
.courts-section,
.court-detail,
.reservations-section,
.admin-panel {
  max-width: 1200px;
}
.popular-courts {
  background: transparent;
}
.feature-card,
.court-card,
.court-amenities-section,
.booking-section,
.auth-card,
.reservation-card,
.admin-section,
.schedule-board,
.side-card,
.modal-content,
.success-modal,
.admin-mobile-card,
.mobile-court-card {
  background: var(--surface);
  border: 1px solid var(--border-soft);
  box-shadow: var(--shadow);
  -webkit-backdrop-filter: blur(12px);
  backdrop-filter: blur(12px);
}
.feature-card,
.court-card,
.reservation-card,
.auth-card,
.booking-section,
.admin-section,
.schedule-board,
.side-card {
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease,
    border-color 0.2s ease;
}
.feature-card:hover,
.court-card:hover,
.reservation-card:hover {
  border-color: rgba(15, 143, 127, 0.18);
  box-shadow: var(--shadow-lg);
}
.feature-icon {
  width: 64px;
  height: 64px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-xl);
  background:
    linear-gradient(
      135deg,
      rgba(15, 143, 127, 0.12),
      rgba(245, 158, 11, 0.12));
  font-size: 2rem;
}
.court-image::after,
.court-image-large::after {
  content: "";
  position: absolute;
  inset: auto 0 0;
  height: 42%;
  background:
    linear-gradient(
      180deg,
      transparent,
      rgba(11, 23, 19, 0.34));
  pointer-events: none;
}
.court-type,
.inactive-badge,
.court-type-badge,
.amenity-tag,
.amenity-badge,
.type-badge,
.status-badge,
.reservation-status,
.guest-badge {
  border-radius: 999px;
  font-weight: 800;
  letter-spacing: 0;
}
.court-type,
.court-type-badge,
.amenity-badge,
.type-badge {
  background: rgba(15, 143, 127, 0.92);
  color: var(--white);
  box-shadow: 0 12px 22px -18px rgba(11, 23, 19, 0.9);
}
.amenity-tag {
  background: rgba(15, 143, 127, 0.08);
  color: var(--primary-dark);
  border: 1px solid rgba(15, 143, 127, 0.12);
}
.court-info h3,
.reservation-title h3,
.side-card h3,
.admin-mobile-title-row h3 {
  color: var(--gray-900);
}
.court-footer,
.reservation-footer,
.auth-footer,
.summary-row.total {
  border-color: var(--border-soft);
}
.court-price,
.stat-value {
  color: var(--primary-dark);
}
.stats {
  background:
    linear-gradient(
      135deg,
      var(--gray-900),
      var(--primary-dark));
}
.stat-item {
  border: 1px solid rgba(255, 255, 255, 0.14);
  border-radius: var(--radius-xl);
  background: rgba(255, 255, 255, 0.08);
}
.filters,
.reservations-tabs,
.admin-tabs,
.schedule-toolbar {
  background: rgba(255, 255, 255, 0.62);
  border: 1px solid var(--border-soft);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-sm);
}
.filters {
  margin-top: 1.5rem;
}
.tab-btn {
  border-color: var(--border-soft);
  background: rgba(255, 255, 255, 0.72);
  font-weight: 750;
}
.tab-btn.active {
  background: var(--gray-900);
  border-color: var(--gray-900);
  color: var(--white);
  box-shadow: 0 14px 26px -18px rgba(11, 23, 19, 0.8);
}
.page-header {
  background:
    linear-gradient(
      120deg,
      rgba(11, 23, 19, 0.92),
      rgba(18, 63, 45, 0.78) 52%,
      rgba(194, 65, 12, 0.38)),
    url(https://images.pexels.com/photos/1578750/pexels-photo-1578750.jpeg?auto=compress&cs=tinysrgb&w=1800);
  padding: 4.75rem 1.5rem;
}
.page-header h1 {
  font-size: clamp(2rem, 4vw, 3.4rem);
}
.booking-summary,
.booking-contact,
.guest-booking,
.login-prompt,
.demo-credentials,
.success-details {
  background: var(--surface-muted);
  border: 1px solid var(--border-soft);
}
.time-slot,
.payment-option,
.recent-row {
  border-color: var(--border-soft);
  background: rgba(255, 255, 255, 0.78);
}
.time-slot.available:hover,
.payment-option.selected,
.recent-row:hover {
  border-color: rgba(15, 143, 127, 0.34);
  background: rgba(15, 143, 127, 0.07);
}
.time-slot.selected,
.schedule-booking {
  background:
    linear-gradient(
      135deg,
      var(--primary),
      var(--primary-dark));
}
.auth-page {
  background:
    radial-gradient(
      circle at 50% 20%,
      rgba(15, 143, 127, 0.13),
      transparent 24rem),
    transparent;
}
.schedule-hero {
  background:
    linear-gradient(
      135deg,
      rgba(11, 23, 19, 0.95),
      rgba(10, 109, 97, 0.9)),
    url(https://images.pexels.com/photos/3660204/pexels-photo-3660204.jpeg?auto=compress&cs=tinysrgb&w=1400);
  background-size: cover;
  background-position: center;
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: var(--shadow-lg);
}
.schedule-eyebrow {
  color: rgba(255, 255, 255, 0.68);
}
.schedule-stat,
.stat-card {
  background:
    linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.92),
      rgba(248, 250, 248, 0.8));
  border: 1px solid var(--border-soft);
  box-shadow: var(--shadow);
}
.schedule-cell {
  border-color: rgba(220, 230, 225, 0.78);
}
.schedule-head,
.data-table th {
  background: #f5f8f6;
  color: var(--gray-700);
}
.data-table {
  border: 1px solid var(--border-soft);
  border-radius: var(--radius-lg);
  overflow: hidden;
  background: var(--white);
}
.data-table td {
  border-color: var(--gray-100);
}
.data-table tbody tr:hover {
  background: rgba(15, 143, 127, 0.04);
}
.modal-overlay,
.success-overlay {
  background: rgba(8, 16, 13, 0.58);
  -webkit-backdrop-filter: blur(8px);
  backdrop-filter: blur(8px);
}
.footer {
  background:
    linear-gradient(
      135deg,
      rgba(11, 23, 19, 0.96),
      rgba(16, 35, 31, 0.98)),
    radial-gradient(
      circle at top right,
      rgba(45, 212, 191, 0.18),
      transparent 24rem);
}
.footer-content {
  padding-top: 2.5rem;
  padding-bottom: 2.5rem;
}
@media (max-width: 768px) {
  body {
    -webkit-text-size-adjust: 100%;
  }
  .btn,
  .input,
  .select,
  .textarea,
  .select-sm,
  .time-slot,
  .payment-option {
    min-height: 44px;
  }
  .input,
  .select,
  .textarea {
    font-size: 16px;
  }
  .main-content {
    overflow-x: hidden;
    padding-bottom: env(safe-area-inset-bottom);
  }
  .footer {
    margin-top: 1.5rem;
  }
  .footer-content {
    grid-template-columns: 1fr;
    gap: 1.25rem;
    padding: 1.5rem 1rem;
  }
  .footer-bottom {
    padding: 1rem;
  }
  .admin-panel {
    padding: 1rem;
  }
  .admin-header {
    margin-bottom: 1rem;
  }
  .admin-header h1 {
    font-size: 1.45rem;
  }
  .admin-header p {
    font-size: 0.92rem;
  }
  .admin-tabs {
    margin: 0 -1rem 1rem;
    padding: 0 1rem 0.35rem;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
  }
  .admin-tabs::-webkit-scrollbar {
    display: none;
  }
  .admin-tabs .tab-btn {
    flex: 0 0 auto;
    min-height: 44px;
    white-space: nowrap;
  }
  .header-content {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 0.75rem;
    padding: 0.7rem 1rem;
    overflow: hidden;
  }
  .logo {
    min-width: 0;
  }
  .nav {
    grid-column: 1 / -1;
    order: 3;
    flex-wrap: nowrap;
    justify-content: flex-start;
    margin: 0 -1rem -0.25rem;
    padding: 0 1rem 0.25rem;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
  }
  .nav::-webkit-scrollbar {
    display: none;
  }
  .nav a {
    flex: 0 0 auto;
    min-height: 40px;
    display: inline-flex;
    align-items: center;
    padding: 0.55rem 0.85rem;
    font-size: 0.9rem;
  }
  .hero h1 {
    font-size: 2rem;
  }
  .hero p {
    font-size: 1rem;
  }
  .page-header h1 {
    font-size: 1.75rem;
  }
  .page-header {
    padding: 2rem 1rem;
  }
  .page-header p {
    max-width: 32rem;
    margin-left: auto;
    margin-right: auto;
  }
  .filters {
    padding: 1rem;
    gap: 1rem;
    align-items: stretch;
  }
  .filter-group {
    width: 100%;
    align-items: stretch;
    flex-direction: column;
    gap: 0.5rem;
  }
  .filter-buttons {
    flex-wrap: nowrap;
    margin: 0 -1rem;
    padding: 0 1rem 0.25rem;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
  }
  .filter-buttons::-webkit-scrollbar {
    display: none;
  }
  .filter-buttons .btn {
    flex: 0 0 auto;
  }
  .courts-section,
  .court-detail,
  .reservations-section {
    padding-left: 1rem;
    padding-right: 1rem;
  }
  .courts-grid {
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 280px), 1fr));
    gap: 1rem;
  }
  .court-card:hover,
  .feature-card:hover {
    transform: none;
  }
  .court-image-large {
    height: 250px;
  }
  .booking-section {
    padding: 1.1rem;
    border-radius: var(--radius-lg);
  }
  .payment-options {
    grid-template-columns: 1fr;
  }
  .summary-row {
    gap: 1rem;
  }
  .success-modal,
  .modal-content {
    max-height: calc(100vh - 2rem);
    overflow-y: auto;
    padding: 1.25rem;
  }
  .court-quick-info {
    flex-direction: column;
    gap: 0.75rem;
  }
  .reservations-details {
    grid-template-columns: 1fr;
  }
  .reservations-tabs {
    margin: 0 -1rem 1rem;
    padding: 0 1rem 0.2rem;
    overflow-x: auto;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
  }
  .reservations-tabs::-webkit-scrollbar {
    display: none;
  }
  .reservations-tabs .tab-btn {
    flex: 0 0 auto;
    min-height: 44px;
  }
  .data-table {
    display: block;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
  }
  .data-table table,
  table.data-table {
    min-width: 720px;
  }
  .schedule-hero,
  .schedule-toolbar,
  .section-actions {
    flex-direction: column;
  }
  .admin-section {
    padding: 1rem;
    border-radius: var(--radius-lg);
  }
  .section-actions {
    align-items: stretch;
    margin-bottom: 1rem;
  }
  .section-actions > .btn,
  .section-actions > button {
    width: 100%;
  }
  .filters-inline {
    display: grid;
    grid-template-columns: 1fr;
    width: 100%;
  }
  .filters-inline .select,
  .filters-inline .input,
  .filters-inline .btn {
    width: 100%;
    min-width: 0;
  }
  .courts-table,
  .reservations-table {
    display: none;
  }
  .admin-mobile-list {
    display: grid;
    gap: 0.85rem;
  }
  .admin-mobile-card {
    padding: 0.9rem;
  }
  .schedule-stats,
  .schedule-layout {
    grid-template-columns: 1fr;
  }
  .schedule-actions {
    justify-content: flex-start;
  }
  .schedule-hero {
    padding: 1.1rem;
    border-radius: var(--radius-lg);
  }
  .schedule-hero h2 {
    font-size: 1.55rem;
  }
  .schedule-actions .btn {
    min-height: 44px;
  }
  .schedule-stat {
    padding: 1rem;
  }
  .schedule-toolbar {
    gap: 0.75rem;
    padding: 0.85rem;
  }
  .schedule-date,
  .schedule-filter {
    min-width: 0;
  }
  .schedule-calendar {
    display: none;
  }
  .schedule-mobile {
    display: grid;
    gap: 0.85rem;
    padding: 0.85rem;
  }
  .mobile-court-card {
    overflow: hidden;
    border: 1px solid var(--gray-200);
    border-radius: var(--radius-lg);
    background: var(--white);
  }
  .mobile-court-card header {
    display: flex;
    justify-content: space-between;
    gap: 0.75rem;
    padding: 0.9rem;
    background: var(--gray-50);
    border-bottom: 1px solid var(--gray-200);
  }
  .mobile-court-card header strong,
  .mobile-court-card header span {
    display: block;
  }
  .mobile-court-card header strong {
    color: var(--gray-900);
  }
  .mobile-court-card header span {
    margin-top: 0.15rem;
    color: var(--gray-500);
    font-size: 0.8rem;
    text-transform: capitalize;
  }
  .mobile-court-status {
    align-self: flex-start;
    padding: 0.25rem 0.55rem;
    border-radius: 999px;
    background: #dcfce7;
    color: #166534 !important;
    font-size: 0.72rem !important;
    font-weight: 800;
  }
  .mobile-slot-list {
    display: grid;
  }
  .mobile-slot {
    display: grid;
    grid-template-columns: 64px 1fr;
    gap: 0.75rem;
    align-items: center;
    width: 100%;
    min-height: 56px;
    padding: 0.75rem 0.9rem;
    border: 0;
    border-bottom: 1px solid var(--gray-100);
    background: var(--white);
    color: var(--gray-800);
    text-align: left;
    cursor: pointer;
  }
  .mobile-slot:last-child {
    border-bottom: 0;
  }
  .mobile-slot-time {
    color: var(--gray-500);
    font-size: 0.82rem;
    font-weight: 800;
    font-variant-numeric: tabular-nums;
  }
  .mobile-slot-main strong,
  .mobile-slot-main small {
    display: block;
  }
  .mobile-slot-main small {
    margin-top: 0.1rem;
    color: var(--gray-500);
    font-size: 0.78rem;
  }
  .mobile-slot.free {
    background: #f8fafc;
  }
  .mobile-slot.free .mobile-slot-main strong {
    color: #166534;
  }
  .mobile-slot.booked {
    background: #eef2ff;
    box-shadow: inset 4px 0 0 var(--primary);
  }
  .mobile-slot.booked.paid {
    background: #ecfdf5;
    box-shadow: inset 4px 0 0 #0d9488;
  }
  .mobile-slot.booked.pending {
    background: #fffbeb;
    box-shadow: inset 4px 0 0 #f59e0b;
  }
  .mobile-slot.booked.completed {
    background: #f1f5f9;
    box-shadow: inset 4px 0 0 #64748b;
  }
  .schedule-side {
    gap: 0.85rem;
  }
  .detail-stack p {
    align-items: flex-start;
    flex-direction: column;
    gap: 0.2rem;
  }
  .detail-stack strong {
    text-align: left;
    overflow-wrap: anywhere;
  }
  .form-row {
    grid-template-columns: 1fr;
  }
  .admin-actions {
    flex-direction: column;
  }
}
@media (max-width: 480px) {
  .logo-icon {
    display: none;
  }
  .logo {
    font-size: 1.05rem;
  }
  .user-info .user-name {
    display: none;
  }
  .user-section,
  .user-info {
    min-width: 0;
  }
  .user-section {
    justify-content: flex-end;
  }
  .user-section .btn,
  .user-info .btn {
    flex: 0 1 auto;
    max-width: 9.5rem;
    padding-left: 0.85rem;
    padding-right: 0.85rem;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .hero {
    padding: 3.5rem 1rem;
  }
  .features,
  .popular-courts,
  .stats {
    padding: 2.25rem 1rem;
  }
  .feature-card,
  .court-info,
  .court-amenities-section,
  .guest-booking,
  .booking-contact {
    padding: 1rem;
  }
  .court-footer,
  .reservation-footer,
  .modal-actions {
    align-items: stretch;
    flex-direction: column;
  }
  .court-footer .btn,
  .reservation-footer .btn,
  .modal-actions .btn {
    width: 100%;
  }
  .reservation-header {
    gap: 0.75rem;
    align-items: flex-start;
    flex-direction: column;
  }
  .reservation-title {
    width: 100%;
  }
  .time-slots {
    grid-template-columns: repeat(2, 1fr);
    gap: 0.55rem;
  }
  .time-slot {
    min-height: 52px;
    padding: 0.75rem 0.45rem;
    font-size: 0.86rem;
  }
  .admin-mobile-card-top {
    align-items: flex-start;
  }
  .admin-mobile-thumb {
    width: 68px;
    height: 68px;
    flex-basis: 68px;
  }
  .admin-mobile-title-row {
    align-items: flex-start;
    flex-direction: column;
    gap: 0.45rem;
  }
  .admin-mobile-actions,
  .admin-mobile-controls {
    grid-template-columns: 1fr;
  }
  .admin-mobile-detail-grid p {
    flex-direction: column;
    gap: 0.15rem;
  }
  .admin-mobile-detail-grid strong {
    text-align: left;
  }
  .schedule-stats {
    gap: 0.65rem;
  }
  .schedule-stat strong {
    font-size: 1.55rem;
  }
  .mobile-slot {
    grid-template-columns: 54px 1fr;
    padding: 0.72rem;
  }
  .quick-row,
  .quick-row .select-sm {
    width: 100%;
  }
}

/* angular:styles/global:styles */
/*# sourceMappingURL=styles.css.map */
